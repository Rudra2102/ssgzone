$f = 'd:\Pradeep_Singh\Creations\Softwares\SSGzone\unified-login\src\WebmailDashboard.js'
$c = [System.IO.File]::ReadAllText($f)

# 1. Add rules state after oooEditing state
$old1 = "  const [oooEditing, setOooEditing] = useState(false);"
$new1 = "  const [oooEditing, setOooEditing] = useState(false);`n  const [rules, setRules] = useState([]);`n  const [rulesLoading, setRulesLoading] = useState(false);`n  const [ruleForm, setRuleForm] = useState({ name: '', condition_field: 'from', condition_operator: 'contains', condition_value: '', action_type: 'move', action_value: 'spam' });`n  const [ruleFormOpen, setRuleFormOpen] = useState(false);`n  const [ruleSaving, setRuleSaving] = useState(false);`n  const [applyingRules, setApplyingRules] = useState(false);"
$c = $c.Replace($old1, $new1)

# 2. Add rules functions before fetchOoo
$old2 = "  const fetchOoo = async () => {"
$new2 = "  const fetchRules = async () => {`n    setRulesLoading(true);`n    try {`n      const res = await fetch('https://api.ssgzone.in/api/v1/rules', { headers: auth });`n      const data = await res.json();`n      if (data.success) setRules(data.data);`n    } catch {}`n    setRulesLoading(false);`n  };`n`n  const saveRule = async () => {`n    if (!ruleForm.name || !ruleForm.condition_value) return alert('Name and condition value required');`n    if (ruleForm.action_type === 'move' && !ruleForm.action_value) return alert('Destination folder required for move action');`n    setRuleSaving(true);`n    try {`n      const res = await fetch('https://api.ssgzone.in/api/v1/rules', {`n        method: 'POST',`n        headers: { ...auth, 'Content-Type': 'application/json' },`n        body: JSON.stringify(ruleForm)`n      });`n      const data = await res.json();`n      if (data.success) {`n        setRules(prev => [...prev, data.data]);`n        setRuleFormOpen(false);`n        setRuleForm({ name: '', condition_field: 'from', condition_operator: 'contains', condition_value: '', action_type: 'move', action_value: 'spam' });`n      } else alert(data.error);`n    } catch (err) { alert(err.message); }`n    setRuleSaving(false);`n  };`n`n  const deleteRule = async (id) => {`n    if (!window.confirm('Delete this rule?')) return;`n    await fetch(`+"`"+`https://api.ssgzone.in/api/v1/rules/`+"`"+`${id}`+"`"+``, { method: 'DELETE', headers: auth });`n    setRules(prev => prev.filter(r => r.id !== id));`n  };`n`n  const toggleRule = async (id) => {`n    const res = await fetch(`+"`"+`https://api.ssgzone.in/api/v1/rules/`+"`"+`${id}`+"`"+`/toggle`, { method: 'PATCH', headers: auth });`n    const data = await res.json();`n    if (data.success) setRules(prev => prev.map(r => r.id === id ? data.data : r));`n  };`n`n  const applyRules = async () => {`n    setApplyingRules(true);`n    try {`n      const res = await fetch('https://api.ssgzone.in/api/v1/rules/apply', { method: 'POST', headers: auth });`n      const data = await res.json();`n      if (data.success) {`n        alert(`+"`"+`Rules applied - `+"`"+`${data.affected}`+"`"+` email(s) updated`+"`"+`);`n        fetchEmails();`n        fetchFolderCounts();`n      } else alert(data.error);`n    } catch (err) { alert(err.message); }`n    setApplyingRules(false);`n  };`n`n  const fetchOoo = async () => {"
$c = $c.Replace($old2, $new2)

# 3. Add rules nav item before OOO nav item
$old3 = "          <div onClick={() => { setActiveNav('ooo'); fetchOoo(); }}"
$new3 = "          <div onClick={() => { setActiveNav('rules'); fetchRules(); }}`n            style={{ display: 'flex', alignItems: 'center', gap: 8, padding: '8px 10px', borderRadius: 6, cursor: 'pointer', background: activeNav === 'rules' ? c.primaryLight : 'transparent', color: activeNav === 'rules' ? c.primary : c.text, fontWeight: activeNav === 'rules' ? 600 : 400, fontSize: 13, justifyContent: sidebarCollapsed ? 'center' : 'flex-start' }}>`n            <span style={{ fontSize: 14 }}>&#9881;&#65039;</span>`n            {!sidebarCollapsed && <span>Email Rules</span>}`n          </div>`n          <div onClick={() => { setActiveNav('ooo'); fetchOoo(); }}"
$c = $c.Replace($old3, $new3)

# 4. Add rules panel before OOO panel
$old4 = "        ) : activeNav === 'ooo' ? ("
$new4 = "        ) : activeNav === 'rules' ? (`n          <div style={{ flex: 1, overflowY: 'auto', padding: 24 }}>`n            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: 20 }}>`n              <div>`n                <div style={{ fontSize: 20, fontWeight: 700, color: c.text }}>Email Rules</div>`n                <div style={{ fontSize: 13, color: c.textMuted }}>Auto-sort, star, or mark emails based on conditions</div>`n              </div>`n              <div style={{ display: 'flex', gap: 8 }}>`n                <button onClick={applyRules} disabled={applyingRules}`n                  style={{ padding: '9px 16px', border: `+"`"+`1px solid `+"`"+`${c.border}`+"`"+``, borderRadius: 8, background: 'none', cursor: applyingRules ? 'not-allowed' : 'pointer', fontSize: 13, color: c.text, opacity: applyingRules ? 0.6 : 1 }}>`n                  {applyingRules ? 'Applying...' : '&#9654; Apply to Inbox'}`n                </button>`n                <button onClick={() => setRuleFormOpen(true)}`n                  style={{ background: 'linear-gradient(135deg, #6366f1, #8b5cf6)', color: '#fff', border: 'none', borderRadius: 8, padding: '9px 18px', fontSize: 13, fontWeight: 600, cursor: 'pointer' }}>`n                  + New Rule`n                </button>`n              </div>`n            </div>`n`n            {ruleFormOpen && (`n              <div style={{ background: c.card, border: `+"`"+`1px solid `+"`"+`${c.border}`+"`"+``, borderRadius: 12, padding: 20, marginBottom: 20 }}>`n                <div style={{ fontSize: 14, fontWeight: 700, color: c.text, marginBottom: 14 }}>New Rule</div>`n                <input value={ruleForm.name} onChange={e => setRuleForm(p => ({ ...p, name: e.target.value }))}`n                  placeholder=`+'"'+'Rule name *'+'"'+``n                  style={{ width: '100%', padding: '9px 12px', border: `+"`"+`1px solid `+"`"+`${c.border}`+"`"+``, borderRadius: 7, fontSize: 13, marginBottom: 10, outline: 'none', boxSizing: 'border-box' }} />`n                <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr', gap: 10, marginBottom: 10 }}>`n                  <select value={ruleForm.condition_field} onChange={e => setRuleForm(p => ({ ...p, condition_field: e.target.value }))}`n                    style={{ padding: '9px 12px', border: `+"`"+`1px solid `+"`"+`${c.border}`+"`"+``, borderRadius: 7, fontSize: 13, outline: 'none', background: '#fff' }}>`n                    <option value=`+'"from"'+`>From</option>`n                    <option value=`+'"subject"'+`>Subject</option>`n                    <option value=`+'"body"'+`>Body</option>`n                  </select>`n                  <select value={ruleForm.condition_operator} onChange={e => setRuleForm(p => ({ ...p, condition_operator: e.target.value }))}`n                    style={{ padding: '9px 12px', border: `+"`"+`1px solid `+"`"+`${c.border}`+"`"+``, borderRadius: 7, fontSize: 13, outline: 'none', background: '#fff' }}>`n                    <option value=`+'"contains"'+`>contains</option>`n                    <option value=`+'"equals"'+`>equals</option>`n                    <option value=`+'"starts_with"'+`>starts with</option>`n                  </select>`n                  <input value={ruleForm.condition_value} onChange={e => setRuleForm(p => ({ ...p, condition_value: e.target.value }))}`n                    placeholder=`+'"Value *"'+``n                    style={{ padding: '9px 12px', border: `+"`"+`1px solid `+"`"+`${c.border}`+"`"+``, borderRadius: 7, fontSize: 13, outline: 'none' }} />`n                </div>`n                <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 10, marginBottom: 14 }}>`n                  <select value={ruleForm.action_type} onChange={e => setRuleForm(p => ({ ...p, action_type: e.target.value, action_value: e.target.value === 'move' ? 'spam' : '' }))}`n                    style={{ padding: '9px 12px', border: `+"`"+`1px solid `+"`"+`${c.border}`+"`"+``, borderRadius: 7, fontSize: 13, outline: 'none', background: '#fff' }}>`n                    <option value=`+'"move"'+`>Move to folder</option>`n                    <option value=`+'"star"'+`>Star</option>`n                    <option value=`+'"mark_read"'+`>Mark as read</option>`n                    <option value=`+'"mark_unread"'+`>Mark as unread</option>`n                  </select>`n                  {ruleForm.action_type === 'move' && (`n                    <select value={ruleForm.action_value} onChange={e => setRuleForm(p => ({ ...p, action_value: e.target.value }))}`n                      style={{ padding: '9px 12px', border: `+"`"+`1px solid `+"`"+`${c.border}`+"`"+``, borderRadius: 7, fontSize: 13, outline: 'none', background: '#fff' }}>`n                      <option value=`+'"spam"'+`>Spam</option>`n                      <option value=`+'"trash"'+`>Trash</option>`n                      <option value=`+'"drafts"'+`>Drafts</option>`n                      <option value=`+'"inbox"'+`>Inbox</option>`n                    </select>`n                  )}`n                </div>`n                <div style={{ display: 'flex', gap: 10 }}>`n                  <button onClick={saveRule} disabled={ruleSaving}`n                    style={{ background: 'linear-gradient(135deg, #6366f1, #8b5cf6)', color: '#fff', border: 'none', borderRadius: 7, padding: '9px 20px', fontSize: 13, fontWeight: 600, cursor: ruleSaving ? 'not-allowed' : 'pointer', opacity: ruleSaving ? 0.7 : 1 }}>`n                    {ruleSaving ? 'Saving...' : 'Save Rule'}`n                  </button>`n                  <button onClick={() => setRuleFormOpen(false)}`n                    style={{ background: 'none', border: `+"`"+`1px solid `+"`"+`${c.border}`+"`"+``, borderRadius: 7, padding: '9px 16px', fontSize: 13, cursor: 'pointer', color: c.textMuted }}>`n                    Cancel`n                  </button>`n                </div>`n              </div>`n            )}`n`n            {rulesLoading && <div style={{ color: c.textMuted, fontSize: 13, padding: 20 }}>Loading...</div>}`n`n            {!rulesLoading && rules.length === 0 && !ruleFormOpen && (`n              <div style={{ textAlign: 'center', padding: '60px 0' }}>`n                <div style={{ fontSize: 48, marginBottom: 12 }}>&#9881;&#65039;</div>`n                <div style={{ fontSize: 15, fontWeight: 600, color: c.text, marginBottom: 6 }}>No rules yet</div>`n                <div style={{ fontSize: 13, color: c.textMuted }}>Create rules to automatically sort, star, or mark your emails</div>`n              </div>`n            )}`n`n            {rules.map(rule => (`n              <div key={rule.id} style={{ background: c.card, border: `+"`"+`1px solid `+"`"+`${c.border}`+"`"+``, borderRadius: 10, padding: '14px 18px', marginBottom: 10, display: 'flex', alignItems: 'center', gap: 14 }}>`n                <div onClick={() => toggleRule(rule.id)}`n                  style={{ width: 40, height: 22, borderRadius: 11, background: rule.is_active ? '#10b981' : c.border, cursor: 'pointer', position: 'relative', flexShrink: 0, transition: 'background 0.2s' }}>`n                  <div style={{ width: 16, height: 16, borderRadius: '50%', background: '#fff', position: 'absolute', top: 3, left: rule.is_active ? 21 : 3, transition: 'left 0.2s' }} />`n                </div>`n                <div style={{ flex: 1 }}>`n                  <div style={{ fontSize: 13, fontWeight: 600, color: c.text }}>{rule.name}</div>`n                  <div style={{ fontSize: 12, color: c.textMuted, marginTop: 2 }}>`n                    If <strong>{rule.condition_field}</strong> {rule.condition_operator.replace('_', ' ')} "<strong>{rule.condition_value}</strong>"`n                    {' -> '}<strong>{rule.action_type === 'move' ? `+"`"+`move to `+"`"+`${rule.action_value}`+"`"+` : rule.action_type.replace('_', ' ')}</strong>`n                  </div>`n                </div>`n                <button onClick={() => deleteRule(rule.id)}`n                  style={{ padding: '6px 12px', border: `+"`"+`1px solid `+"`"+`${c.danger}`+"`"+``, borderRadius: 6, background: 'none', cursor: 'pointer', fontSize: 12, color: c.danger }}>`n                  Delete`n                </button>`n              </div>`n            ))}`n          </div>`n        ) : activeNav === 'ooo' ? ("
$c = $c.Replace($old4, $new4)

[System.IO.File]::WriteAllText($f, $c)
Write-Host "Done. Contains fetchRules: $($c.Contains('fetchRules')). Contains rules panel: $($c.Contains('Email Rules'))"
